---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-longhorn
  namespace: longhorn-system 
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`v1.lan`)
      services:
        - kind: Service
          name: longhorn-frontend
          port: 80
