---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: gitea-backup
spec:
  # Every day at 0200
  schedule: "10 12 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: borg 
              image: pschiffe/borg:latest
              env:
                - name: TZ
                  value: "America/Los_Angeles"
                - name: BORG_REPO
                  value: "tstarr@69.69.1.14:/engi/store/vortex/gitea.borg"
                - name: BORG_RSH
                  value: "ssh -o StrictHostKeyChecking=no -oBatchMode=yes -i /secrets/torus_ssh_key"
                - name: ARCHIVE
                  value: "gitea-{now}"
                - name: BORG_PASSPHRASE
                  valueFrom:
                    secretKeyRef:
                      name: sops-apps-dev
                      key: VORTEX_BORG_PASSPHRASE
                - name: BACKUP_DIRS
                  value: "/data"
              volumeMounts:
                - name: borg-secrets-volume
                  readOnly: true
                  mountPath: /secrets
                - name: gitea-volume
                  readOnly: true
                  mountPath: /data
          volumes:
            - name: borg-secrets-volume
              secret:
                secretName: sops-apps-dev
                defaultMode: 0400
                items:
                  - key: TORUS_SSH_KEY
                    path: torus_ssh_key
            - name: gitea-volume
              persistentVolumeClaim:
                claimName: gitea-pvc
